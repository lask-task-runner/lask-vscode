# lask-vscode
Lask language support for VS Code.

## Build

Build a distributable VS Code package (`.vsix`):

```bash
npm ci
npm run vscode:package
```

Generated package:

- `vscode-lask-0.0.1.vsix`
